1.// Template string
const student = {
    name: 'Ringky',
    id:15203016,
    address:'Mohammod Pur'
}

const about =`My Nmae is ${student.name} id is ${student.id} address is ${student.address}`;
console.log(about);